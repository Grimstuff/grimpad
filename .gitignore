# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & frontend build output
node_modules
dist
dist-ssr
*.local

# Env / secrets (never commit)
.env
.env.*
!.env.example

# Tauri / Rust build artifacts
src-tauri/target
src-tauri/gen
src-tauri/WixTools
*.pdb
*.exe
*.msi
!scripts/**

# Staging copies for GitHub uploads (local only)
**/github-assets/
release/
releases/

# Legacy AMD Monaco copy (no longer used; safe to delete locally)
public/monaco

# Mobile icon trees if `tauri icon` regenerates them (Windows-first app)
src-tauri/icons/ios/
src-tauri/icons/android/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~

# Misc tooling / caches
*.tsbuildinfo
.turbo
coverage
.cache
.parcel-cache
.eslintcache
*.orig
*.bak
